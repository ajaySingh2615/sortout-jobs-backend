# Production profile — active with: spring.profiles.active=prod
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate
# Ensure all secrets and URLs come from environment (no defaults in prod)
